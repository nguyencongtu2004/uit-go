apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: mongodb-users
  labels:
    app: mongodb-users
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb-users
  clusterIP: None 
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: trip-service
  labels:
    app: trip-service
spec:
  type: ClusterIP
  selector:
    app: trip-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http

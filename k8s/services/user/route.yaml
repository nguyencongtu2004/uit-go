apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  namespace: default
  name: user-service-route
  labels:
    app: user-service
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`user.localhost`) || PathPrefix(`/api/users`) || PathPrefix(`/api/auth`)
      kind: Rule
      services:
        - name: user-service
          port: 3000
      middlewares:
        - name: api-middleware
          namespace: default
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: api-middleware
  labels:
    app: user-service
spec:
  chain:
    middlewares:
      - name: api-cors
      - name: compression
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: api-cors
  labels:
    app: user-service
spec:
  headers:
    accessControlAllowMethods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS
    accessControlAllowOriginList:
      - "*"
    accessControlAllowHeaders:
      - "Content-Type"
      - "Authorization"
      - "X-Requested-With"
      - "Accept"
      - "Origin"
    accessControlAllowCredentials: true
    accessControlMaxAge: 86400
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: compression
  labels:
    app: user-service
spec:
  compress: {}

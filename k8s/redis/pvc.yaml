apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: redis-pvc
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
